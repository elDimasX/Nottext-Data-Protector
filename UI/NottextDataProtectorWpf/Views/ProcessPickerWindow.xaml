<Window x:Class="Nottext_Data_Protector.Views.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Selecionar processo" Height="620" Width="860"
        MinHeight="520" MinWidth="720"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        WindowStyle="None" Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextPrimaryBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="8"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource WindowOutlineBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="0,0,0,1" MouseLeftButtonDown="ChromeTitleBar_MouseLeftButtonDown">
            <Grid Margin="14,0,10,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Border Width="28" Height="28" CornerRadius="10" Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Center"><TextBlock Text="N" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                <TextBlock Grid.Column="1" Text="Selecionar processo" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="—" Style="{StaticResource ChromeButton}" Click="ChromeMinimizeButton_Click"/>
                    <Button x:Name="ChromeMaximizeButton" Style="{StaticResource ChromeButton}" Click="ChromeMaximizeButton_Click" ToolTip="Maximizar ou restaurar">
                        <Path x:Name="ChromeMaximizeIcon" Width="12" Height="12" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" Fill="Transparent" Data="M3,3 L21,3 L21,21 L3,21 Z"/>
                    </Button>
                    <Button Style="{StaticResource ChromeCloseButton}" Click="ChromeCloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
            <Grid Grid.Row="1" Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Adicionar processo autorizado" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="Escolha um processo em execução. Processos sem caminho acessível aparecem bloqueados para evitar travamentos." Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,18,0,14">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox x:Name="SearchBox" MinHeight="38" TextChanged="SearchBox_TextChanged" ToolTip="Filtrar por nome, PID ou caminho"/>
                        <Button Grid.Column="1" Content="Atualizar" Style="{StaticResource GhostButton}" Margin="10,0,0,0" Click="RefreshButton_Click"/>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource Card}" Padding="12">
                    <Grid>
                        <TextBlock x:Name="EmptyText" Text="Nenhum processo com caminho acessível encontrado." Style="{StaticResource MutedText}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" Visibility="Collapsed"/>
                        <ListView x:Name="ProcessesList" SelectionMode="Extended" Background="Transparent" BorderThickness="0">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1" CornerRadius="16" Padding="14">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource CardItemBrush}"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="42"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                            <Border Width="32" Height="32" CornerRadius="11" Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Center">
                                                <Path Width="17" Height="17" Stretch="Uniform" Fill="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M8,5 L16,5 L16,12 L8,12 Z M5,21 L7,15 L17,15 L19,21 Z"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="12,0,18,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Path}" Style="{StaticResource MutedText}" FontSize="12" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="{DynamicResource FieldBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="10,5" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding PidText}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" FontSize="12"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Grid Grid.Row="3" Margin="0,16,0,0">
                    <TextBlock x:Name="StatusText" Style="{StaticResource MutedText}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancelar" Style="{StaticResource GhostButton}" Click="CancelButton_Click" Margin="0,0,10,0"/>
                        <Button Content="Adicionar selecionados" Style="{StaticResource PrimaryButton}" Click="AddSelectedButton_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>