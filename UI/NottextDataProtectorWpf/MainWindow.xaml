<Window x:Class="Nottext_Data_Protector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Nottext Data Protector" Height="720" Width="1120"
        MinHeight="620" MinWidth="980"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource BgBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="8"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource WindowOutlineBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="0,0,0,1" MouseLeftButtonDown="ChromeTitleBar_MouseLeftButtonDown">
                <Grid Margin="14,0,10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="28" Height="28" CornerRadius="10" Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Center">
                        <TextBlock Text="N" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Grid.Column="1" Text="Nottext Data Protector" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="—" Style="{StaticResource ChromeButton}" Click="ChromeMinimizeButton_Click"/>
                        <Button x:Name="ChromeMaximizeButton" Style="{StaticResource ChromeButton}" Click="ChromeMaximizeButton_Click" ToolTip="Maximizar ou restaurar">
                            <Path x:Name="ChromeMaximizeIcon" Width="12" Height="12" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" Fill="Transparent" Data="M3,3 L21,3 L21,21 L3,21 Z"/>
                        </Button>
                        <Button Style="{StaticResource ChromeCloseButton}" Click="ChromeCloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid x:Name="AppShell" Grid.Row="1" Background="{DynamicResource BgBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="SidebarColumn" Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border x:Name="SidebarShell" Grid.Column="0" Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="0,0,1,0" Padding="18">
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Top">
                            <Grid Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="BrandCard" Height="76" CornerRadius="22" Background="{DynamicResource SurfaceElevatedBrush}" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="BrandIconBox" Width="44" Height="44" CornerRadius="14" Background="{DynamicResource AccentSoftBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock x:Name="BrandIconText" Text="N" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource AccentBrush}"/>
                                        </Border>
                                        <StackPanel x:Name="BrandTextPanel" Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                            <TextBlock Text="Nottext" FontSize="20" FontWeight="Bold"/>
                                            <TextBlock Text="Data Protector" Style="{StaticResource MutedText}" FontSize="12"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Button x:Name="SidebarCompactButton" Grid.Column="1" Width="40" Height="40" MinHeight="40" Padding="0" Style="{StaticResource GhostButton}" Margin="8,0,0,0" Click="SidebarCompactButton_Click" ToolTip="Alternar modo compacto" HorizontalAlignment="Right" VerticalAlignment="Top">
                                    <Grid Width="18" Height="14">
                                        <Path Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2.1" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M1,2 L17,2"/>
                                        <Path Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2.1" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M1,7 L17,7"/>
                                        <Path Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2.1" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M1,12 L17,12"/>
                                    </Grid>
                                </Button>
                            </Grid>

                            <Button x:Name="DashboardButton" Style="{StaticResource NavButton}" Click="DashboardButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Width="18" Height="18" Stretch="Uniform" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0" Data="M3,3 L10,3 L10,10 L3,10 Z M14,3 L21,3 L21,10 L14,10 Z M3,14 L10,14 L10,21 L3,21 Z M14,14 L21,14 L21,21 L14,21 Z"/>
                                    <TextBlock x:Name="DashboardLabel" Text="Início" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="ObjectsButton" Style="{StaticResource NavButton}" Click="ObjectsButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Width="18" Height="18" Stretch="Uniform" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0" Data="M3,5 L9,5 L11,7 L21,7 L21,18 L3,18 Z M5,9 L5,16 L19,16 L19,9 Z"/>
                                    <TextBlock x:Name="ObjectsLabel" Text="Objetos protegidos" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="ProcessesButton" Style="{StaticResource NavButton}" Click="ProcessesButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Width="18" Height="18" Stretch="Uniform" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0" Data="M8,5 L16,5 L16,12 L8,12 Z M5,21 L7,15 L17,15 L19,21 Z"/>
                                    <TextBlock x:Name="ProcessesLabel" Text="Processos autorizados" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="SettingsButton" Style="{StaticResource NavButton}" Click="SettingsButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Width="18" Height="18" Stretch="Uniform" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0" Data="M11,2 L13,2 L14,6 L18,4 L20,6 L18,10 L22,11 L22,13 L18,14 L20,18 L18,20 L14,18 L13,22 L11,22 L10,18 L6,20 L4,18 L6,14 L2,13 L2,11 L6,10 L4,6 L6,4 L10,6 Z M9,9 L15,9 L15,15 L9,15 Z"/>
                                    <TextBlock x:Name="SettingsLabel" Text="Configurações" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="AboutButton" Style="{StaticResource NavButton}" Click="AboutButton_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Width="18" Height="18" Stretch="Uniform" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Margin="0,0,12,0" Data="M6,2 L18,2 L22,6 L22,18 L18,22 L6,22 L2,18 L2,6 Z M11,10 L13,10 L13,18 L11,18 Z M11,6 L13,6 L13,8 L11,8 Z"/>
                                    <TextBlock x:Name="AboutLabel" Text="Sobre" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <Border x:Name="FooterStatusCard" DockPanel.Dock="Bottom" Background="{DynamicResource StatusBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="10,8" MinHeight="66">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="DriverDot" Width="8" Height="8" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="StatusText" Grid.Column="1" Text="Driver conectado" Style="{StaticResource MutedText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                </Grid>
                                <Grid Margin="0,6,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="ProtectionDot" Width="8" Height="8" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock x:Name="ProtectionStatusText" Grid.Column="1" Text="Proteção ligada" Style="{StaticResource MutedText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Border>

                <Grid Grid.Column="1" Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,22">
                        <TextBlock x:Name="PageTitle" Text="Objetos protegidos" FontSize="32" FontWeight="Bold"/>
                        <TextBlock x:Name="PageSubtitle" Text="Gerencie arquivos, pastas e programas protegidos pelo driver." Style="{StaticResource MutedText}" FontSize="15" Margin="0,6,0,0"/>
                    </StackPanel>
                    <Frame x:Name="ContentFrame" Grid.Row="1" NavigationUIVisibility="Hidden" Background="Transparent"/>
                </Grid>

                <StackPanel x:Name="ToastHost" Grid.ColumnSpan="2" Width="360" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,24,24" Panel.ZIndex="99"/>
            </Grid>

            <Grid x:Name="OnboardingShell" Grid.Row="1" Background="{DynamicResource BgBrush}" Visibility="Collapsed" Panel.ZIndex="80">
                <Grid Margin="44">
                    <Frame x:Name="OnboardingFrame" NavigationUIVisibility="Hidden" Background="Transparent"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
